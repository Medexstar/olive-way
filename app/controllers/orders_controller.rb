class OrdersController < ApplicationController
    def new
        #@order = Order.new
        #@order.user_id ||= current_user.id
        #@order_objects = OrderObject.where(user_id: current_user.id, status: 0)
    end
end
